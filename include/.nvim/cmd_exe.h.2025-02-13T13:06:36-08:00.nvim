#include "hlpfuncs.h"
#include <stdlib.h>
#include <string.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <unistd.h>
#include <stdbool.h>

void cmd_exe(char **input,bool background) {
  pid_t pid = fork();

  if (pid == 0) {
    execvp(input[0], input);

  } else {
    if (background) {
      printf("backfround");
    } else {
      int status;

      waitpid(pid, &status, 0);
    }
  }
}
