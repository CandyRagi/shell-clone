#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

#define MAX_INPUT_CWD 3072

int main()

{
  
  char cwd[MAX_INPUT_CWD];
  
  while(true)

  {
    if(getcwd(cwd,sizeof(cwd))!=NULL)
    {
      write(STDOUT_FILENO,cwd,
    }
  }


}
